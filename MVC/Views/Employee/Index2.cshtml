@model string[]
<div>
    <h3 class="display-3">
        Employee List
    </h3>
</div>
<div>
    <h3 class="lead">
        @foreach (var item in Model)
        {
            <p>@item</p>
        }
    </h3>
</div>